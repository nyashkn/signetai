/** Entity types that describe source-native structure rather than semantic knowledge. */
export const SOURCE_NATIVE_TOPOLOGY_ENTITY_TYPES = [
	"source_document",
	"source_folder",
	"source_document_reference",
	"skill",
] as const;

